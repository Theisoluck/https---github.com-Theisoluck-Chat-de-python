# ==============================================
# CONFIGURACIÓN DEL CHAT LAN SEGURO
# ==============================================
# Copia este archivo como .env y edita los valores
# O define estas variables directamente en tu sistema

# SEGURIDAD (OBLIGATORIAS)
# -------------------------
# Clave secreta para cifrado AES (cámbiala por una única y segura)
CHAT_SECRET=una-clave-segura-muy-larga-y-compleja-2024

# Sal en base64 para derivación de clave (genera una única)
# Para generar: python -c "import base64, os; print(base64.b64encode(os.urandom(32)).decode())"
CHAT_SALT_BASE64=U29tZVNhbHRIYXNoZWRCYXNlNjQ=

# Token de descubrimiento para validar servidores en LAN
CHAT_DISCOVERY_TOKEN=chat_lan_v1

# CONFIGURACIÓN DE RED
# --------------------
# Puerto del servidor WebSocket
SERVER_PORT=8765

# Puerto UDP para broadcast de descubrimiento
CHAT_BROADCAST_PORT=9999

# IP del servidor (solo si el descubrimiento automático falla)
# SERVER_IP=192.168.1.100

# CONFIGURACIÓN SSL/TLS
# ---------------------
# Habilitar SSL/TLS (WebSocket Secure - wss://)
# true = conexión segura con certificado
# false = conexión sin cifrado de transporte (solo para pruebas)
USE_SSL=true

# Verificar certificados SSL
# true = verificación completa (para producción con CA)
# false = aceptar certificados autofirmados (para LAN)
SSL_VERIFY=false

# Rutas de certificado y llave privada
SSL_CERT_FILE=server.crt
SSL_KEY_FILE=server.key

# ==============================================
# INSTRUCCIONES RÁPIDAS
# ==============================================
# 
# 1. Windows PowerShell - Definir variables:
#    setx CHAT_SECRET "tu-clave-super-secreta"
#    setx CHAT_SALT_BASE64 "tu-sal-en-base64"
#    setx CHAT_DISCOVERY_TOKEN "chat_lan_v1"
#    setx USE_SSL "true"
#    setx SSL_VERIFY "false"
#
# 2. Linux/Mac - Definir variables:
#    export CHAT_SECRET="tu-clave-super-secreta"
#    export CHAT_SALT_BASE64="tu-sal-en-base64"
#    export CHAT_DISCOVERY_TOKEN="chat_lan_v1"
#    export USE_SSL="true"
#    export SSL_VERIFY="false"
#
# 3. Generar certificados SSL (si USE_SSL=true):
#    python generate_ssl_cert.py
#
# 4. Iniciar servidor:
#    python server_ws.py
#
# 5. Iniciar cliente:
#    python client_ws_gui.py
#
# ==============================================
# SEGURIDAD
# ==============================================
# ⚠️ NUNCA compartas CHAT_SECRET en repositorios públicos
# ⚠️ Usa certificados de CA válidos para producción en Internet
# ⚠️ Los certificados autofirmados son SOLO para LAN/desarrollo
# ✅ Cambia CHAT_SECRET y CHAT_SALT_BASE64 por valores únicos
# ✅ Cada instalación debe tener credenciales diferentes
